#include <iostream>
#include "headers/cgrigo.h" // You need to add this line of code in order for it to work

int main()
{
    	if(isPrime(177)) log("Prime!");
	else log("Not Prime!");
	
	// Code
	return 0;
}
